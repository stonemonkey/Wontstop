<UserControl
    x:Class="Wontstop.Ui.Uwp.Views.RunKeeperSessionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding RunKeeperSessionViewModel, Source={StaticResource ViewModelLocator}}">
   
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="RunKeeperConnectButton" 
            Margin="8"
            VerticalAlignment="Top"
            Content="Connect to RunKeeper"
            Command="{Binding ConnectCommand}"
            Visibility="{Binding IsInitialized, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}"
            />
        <Button x:Name="RunKeeperDisconnectButton" 
            Margin="8"
            VerticalAlignment="Top"
            Content="Disconnect from RunKeeper"
            Command="{Binding DisconnectCommand}"
            Visibility="{Binding IsInitialized, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=False}"
            />
    </Grid>
</UserControl>
